# WaterMe
Training app that helps you track wellbeeing of your plants

## Development

No runnable app exists yet. Once the .NET solution is scaffolded, run from the solution root:

- `dotnet restore` — install NuGet dependencies
- `dotnet build` — compile and surface analyzer warnings
- `dotnet test` — run the automated test suite
- `dotnet run` — start the local application
